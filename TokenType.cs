public enum TokenType
{
    Operator,// + , - , * , @ , / , ^
    Number,
    Text,
    Identifier,
    KeyWords, // "funtion" , "if " ,"else ", "let", "in ", "true " ,false "
    Punctuator,// ( , ) , { , } , ; , ", " 
    EndOfLine,
}